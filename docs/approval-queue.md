# 审批队列

当执行模式选择为“应用服务模式”时，Codex 的命令或文件变更审批请求会显示在 dashboard 的“审批”页面。

## 使用方式

1. 进入“高级”。
2. 将执行模式切换为“应用服务模式”。
3. 创建执行任务。
4. 如果 Codex 请求命令或文件变更审批，进入“审批”页面处理。

## 限制

审批队列只适用于通过 Bridge 启动的任务。直接在 Codex Desktop 中单独打开的任务，当前不保证自动同步到 dashboard。

真实项目建议继续使用“人工审查”权限模式。
